spring:
  application:
    name: restaurant-table-microservice
    
  cloud:
    config:
      enabled: true
      uri: http://localhost:9000
      
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/proyectoBackend
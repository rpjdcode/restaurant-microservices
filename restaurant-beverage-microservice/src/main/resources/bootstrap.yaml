spring:
  application:
    name: restaurant-beverage-microservice
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/proyectoBackend
  cloud:
    config:
      enabled: true
      uri: http://localhost:9000
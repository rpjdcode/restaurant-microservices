spring:
  application:
    name: restaurant-gateway-microservice
    
  cloud:
    config:
      enabled: true
      uri: http://localhost:9000
logging:
    level:
        org:
            springframework:
                security: ERROR